name: pull request

on:
  pull_request:
    branches:
      - master

jobs:
  eslint:
    uses: ./.github/workflows/runs/eslint.yml

  prettier:
    uses: ./.github/workflows/runs/prettier.yml

  verify:
    uses: ./.github/workflows/runs/verify.yml
