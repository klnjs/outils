name: publish

on: workflow_call

env:
  NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: publish
        run: yarn run publish
